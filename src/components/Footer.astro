---
import { profile } from "../data/profile";

const year = new Date().getFullYear();

const socials = [
  { href: profile.social.github, label: "GitHub" },
  { href: profile.social.linkedin, label: "LinkedIn" },
];
---

<footer class="mx-auto max-w-4xl px-6 py-10 text-center text-sm text-slate-500">
  <div class="flex justify-center gap-6">
    {
      socials.map((social) => (
        <a
          href={social.href}
          target="_blank"
          rel="noopener noreferrer"
          class="transition-colors hover:text-white"
        >
          {social.label}
        </a>
      ))
    }
  </div>
  <p class="mt-4">
    Diseñado y construido por {profile.name} · {year}
  </p>
</footer>
